<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ComposeLearn" parent="Theme.Material3.Light.NoActionBar" />
    <!-- Base application theme. -->
    <style name="Base.Theme.ComposeLearn" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="TooltipTextAppearance">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorLink">@android:color/holo_blue_light</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>